#!/bin/sh
# Make the /var/log directory executable
chmod -R a+X /var/log
mkdir /var/log/blueprintcode
mkdir a+w /var/log/blueprintcode
mkdir -p /var/run/blueprintcode
mkdir -p /var/run/blueprintcode/
